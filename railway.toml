[build]
buildCommand = "pnpm --filter @workspace/chiefmkt build && pnpm --filter @workspace/api-server build"

[deploy]
startCommand = "sh railway-start.sh"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
