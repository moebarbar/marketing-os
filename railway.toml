[build]
buildCommand = "pnpm --filter @workspace/chiefmkt build && pnpm --filter @workspace/api-server build && pnpm --filter @workspace/db push"

[deploy]
startCommand = "node artifacts/api-server/dist/index.mjs"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
